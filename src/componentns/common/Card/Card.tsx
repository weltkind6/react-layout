import React from 'react';
import styles from './styles.module.css'

const Card = () => {
    return (
            <div className={styles.container}>

            </div>
    );
};

export default Card;
